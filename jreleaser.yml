project:
  name: micrometer-redistimeseries
  description: Micrometer registry for RedisTimeSeries
  longDescription: Micrometer registry for RedisTimeSeries
  links:
    homepage: https://github.com/redis-developer/micrometer-redistimeseries
  authors:
    - Julien Ruaux
  license: Apache-2.0
  inceptionYear: 2020
  java:
    groupId: com.redis
    version: 17
    multiProject: true
  tags:
    - 'micrometer'
    - 'redis'
    - 'timeseries'

release:
  github:
    overwrite: true
    changelog:
      formatted: ALWAYS
      preset: conventional-commits
      format: '- {{commitShortHash}} {{commitTitle}}'
      contributors:
        enabled: false
      labelers:
        - label: 'dependencies'
          title: 'regex:^(?:deps(?:\(.*\))?!?):\s.*'
          order: 130
      categories:
        - title: 'Merge'
          labels:
            - 'merge_pull'
            - 'merge_branch'
        - title: '⚙️  Dependencies'
          key: 'dependencies'
          order: 80
          labels:
            - 'dependencies'
      hide:
        categories:
          - 'Merge'
      replacers:
        - search: 'deps: '

signing:
  active: ALWAYS
  armored: true

deploy:
  maven:
    pomchecker:
      version: early-access
    nexus2:
      maven-central:
        active: RELEASE
        url: https://s01.oss.sonatype.org/service/local
        closeRepository: true
        releaseRepository: true
        stagingRepositories:
          - build/repos/local/release

announce:
  slack:
    active: RELEASE
    channel: '#field-engineering-announcements'
    message: '📏 Micrometer RedisTimeSeries {{projectVersion}} has been released! {{releaseNotesUrl}}'

files:
  artifacts:
    - path: VERSION
      extraProperties:
        skipChecksum: true
        skipSigning: true